
"use strict";

let Detection = require('./Detection.js');
let ContourLabel = require('./ContourLabel.js');
let Detections = require('./Detections.js');
let Detection = require('./Detection.js');
let ContourLabel = require('./ContourLabel.js');
let Detections = require('./Detections.js');

module.exports = {
  Detection: Detection,
  ContourLabel: ContourLabel,
  Detections: Detections,
  Detection: Detection,
  ContourLabel: ContourLabel,
  Detections: Detections,
};
